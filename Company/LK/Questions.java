package LK;

public class Questions {

	/*
	 * 1. color house with 3 colors, minimize the cost. follow up: k colors. 
		2. Design IP black List.
		3. implement data structure insert, delete, random delete both in O(1).
		4. lca have parent pointer, how to find depth of tree node.
		5. Design tinyurl.
		6. Find the size of longest palindrome subset of an array
		   注意是subset而不是subarray。不能改变order。所以[1, 2, 2, 0, 1]的longest palindrome subset是[1, 2, 2, 1]，应该返回4。
	 */
	
}
